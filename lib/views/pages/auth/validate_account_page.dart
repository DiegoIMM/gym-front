import 'package:flutter/material.dart';

class ValidateAccountPage extends StatelessWidget {
  const ValidateAccountPage({required this.token, Key? key}) : super(key: key);

  final String token;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
